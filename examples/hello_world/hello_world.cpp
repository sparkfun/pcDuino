// First, let's include the standard io functions (things like printf, etc)
#include <stdio.h>

int main(void) // main *must* have an int declared as return type!
{
  printf("Hello world!\n"); // print to the command line
}
